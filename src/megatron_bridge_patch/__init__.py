"""megatron_bridge_patch - patch overlay package for megatron.bridge.

Note: This is a partial migration containing only the ``peft`` subpackage
(``grouped_mlp_lora`` and ``lora_patch``). Other subpackages (data, models,
recipes, training, te_patch, etc.) are not included here.
"""
